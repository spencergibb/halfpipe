{
    "builders": [
        {
            "type": "docker",
            "image": "32degrees/consul:0.1",
            "export_path": "consul_server.tar",
            "pull": false
        }
    ],

    "provisioners": [
        {
            "type": "ansible-local",
            "playbook_file": "../ansible/consul_server.yml",
            "role_paths": "../ansible/consul_server"
        }
    ],

    "post-processors": [
        {
            "type": "docker-import",
            "repository": "32degrees/consul_server",
            "tag": "0.1"
        }
    ]
}